# Karen Review: Shanti Kala Nikketan

**Score: 81/100** - "Actually decent" ✅ (8.1/10)

Generated: 2026-09-20 (re-review; prior: 77/100 on 2026-09-16)

## The Reality Check

You fixed the one thing that mattered. On 2026-09-16 the enquiry form was a dead end — `route.ts:111` logged PII to the console and returned `{ok:true}` while a parent thought they'd booked a trial. Commit `466ce4f "Deliver enquiries via Resend"` plus `web/lib/mail.ts:1-124`, `web/lib/enquiry.ts:1-246` and the rewritten `web/app/api/enquiry/route.ts:1-209` closed that hole properly: awaited send, honest 503 in production when `RESEND_API_KEY` is missing (`route.ts:137-144`), 502 with the phone number when delivery fails (`route.ts:155-160`), honeypot (`route.ts:102-104`), validation with 422 (`route.ts:118-130`), per-IP rate limit (`route.ts:54-67`), PII-redacted success logs plus a deliberate unredacted recovery line for lost enquiries (`route.ts:192-205`). The `.env.example:1-22` documents the Resend dance honestly, including the `onboarding@resend.dev` single-recipient trap. That's how you do a contact form.

Don't frame this yet. The route file itself tells you what's still outstanding (`route.ts:26-29`): no Turnstile CAPTCHA, and the limiter is an in-memory `Map` that resets on every serverless cold start. Fine against casual abuse, decorative against a determined spammer. And the git log since last review (`3744293`, `16e0229`, `6e6d48c`, `c50a3fd`) is white-patch fixes, palette tweaks and optimizations with single-line messages — motion, but no evidence of review.

The rest of the old critique still stands, some of it louder:

1. Zero tests. Still no `*.test.*`, no `*.spec.*`, no Playwright spec, no Vitest — verified 2026-09-20. The safety-critical `actVisibility` / `archState` / `verifyActs` in `web/lib/acts.ts:186-287` still fails loudly at module load (good), and `_research/check_act_windows.py` still exists (good), but that's a runtime grenade, not a CI gate. One bad window edit and the homepage is blank with no test to catch it.

2. The README is still `create-next-app` boilerplate (`web/README.md:1-36`). No setup, no env table, no content-editing guide for the academy owner, no deploy target. For a handover site this is the missing manual.

3. Root clutter got worse, not better. `bundled_prompt.txt` is 7,257,005 bytes at root, `mudra.mp4` (3MB) and a 791KB screenshot JPG sit beside `Website Contents.docx`, plus untracked `globefolio-3d-image-animation/` and `.playwright-mcp/console-2026-09-16T07-40-51-147Z.log`. `_research/` (19 Python scripts + crawl artifacts), `_build/` (1655 lines: `build.py`, `content.py`, `layout.py`, `check.py`), `prototype/` (`site.py`, `glyphs.py`, `pose.py`, `morph.py`) triple-track the same site. There is still no root README saying `web/` is canonical.

4. `web/next.config.ts:1-7` is still the empty default — no image remote patterns, no security headers. `web/dev.log` and `web/prod.log` are still checked into the tree. `web/lib/classes.ts:61-63` Canada timings are still provisional (correctly excluded from schema in `web/lib/schema.ts:83-87` — keep that honesty).

What still works: 49 TS/TSX files, 7348 lines in `web/app+components+lib`, `strict:true` (`web/tsconfig.json:7`), zero `as any` / `ts-ignore` / `ts-expect-error`, 25 components, 11 lib modules, 6-act scroll spine with tuned unequal windows (`web/lib/acts.ts:1-37`), `DefinedTermSet` hastas graph (`web/lib/schema.ts:318-354`), derived opening-hours and CourseInstance schedules, `prefers-reduced-motion` fallback, skip-link, sitemap/robots, JSON-LD graphs. The long-form comments explaining *why* (`web/lib/curriculum.ts:1-19`, `web/lib/site.ts:1-8`, `web/lib/mail.ts:1-27`) remain the best part of this codebase.

**Market Context:** Chennai + diaspora Bharatanatyam is saturated — Bharata Kalanjali (since 1968, Dhananjayans), the Kalakshetra Foundation itself, Kalapeetham (36 years), Kshetra/Shivam Narthanalayam/Natya Samarpanam long tail. Nobody wins on tech. This site wins on craft: the six-act mudra scroll, the 28-hastas reference, and SEO that refuses to lie (no fake `aggregateRating`, no invented price, no hallucinated hours). Genuine differentiation for a *client site*. As a *product* it's not reusable — and it was never trying to be.

## Scoring Breakdown

### 🎭 Bullshit Factor: 16/20
Appropriately ambitious. GSAP ScrollTrigger + canvas frame scrub + Lenis (`web/package.json:12-16`) is justified — for a dance academy selling movement, motion IS the product. `TEXT_IN/OUT` ramps, `archState` travel gaps, load-time `verifyActs` show tuning, not gold-plating. Docked 4: one for the surviving prototype/_build/_research triple-track (two rewrites of mileage to land on `web/`), one for the 7.2MB `bundled_prompt.txt` at root, one for untracked `globefolio-3d-image-animation/` sprawl, one for no root map explaining which tree is canonical. Was 17; clutter grew.

### ⚙️ Actually Works: 17/20
Enquiry now actually delivers via Resend with awaited send, PII discipline, honeypot, validation, and honest 5xx + phone fallback — the critical path is real. Static routes, `/locations/[city]`, sitemap/robots, JSON-LD graphs, reduced-motion fallback (`ScrollStage.tsx`), all real. Docked 3: in-memory limiter resets on cold start (self-admitted), no Turnstile server verify (self-admitted), zero automated tests to prove any of it. Was 14; +3 for the Resend fix.

### 💎 Code Quality: 18/20
Clean strict TypeScript, zero `any`/ignores, consistent naming, best-in-class *why* comments (`acts.ts:1-37`, `curriculum.ts:1-19`, `site.ts:1-8`, `mail.ts:1-27`, `route.ts:192-205`). A11y and SEO done properly. Docked 2 for default `next.config.ts:1-7`, `dev.log`/`prod.log` committed, boilerplate README. Was 17; +1 for the excellent new mail/enquiry/route trio.

### ✅ Completion: 15/20
Zero code TODOs for fake success remain; remaining placeholders are honest (`classes.ts` provisional Canada, `schema.ts:80` exclusion, `GOOGLE_FORM_URL=null` in `site.ts:548`). `_research/STATUS.txt:52-109` still lists 7 corrections + 3 open items (unretrieved photo albums, unverified about copy). Missing: Turnstile, durable limiter, any test, real docs, root cleanup. That's the last ~25%. Was 14; +1 for enquiry.

### 🎯 Practical Value: 15/20
Fills a real gap for a real client (est. 2009-04-18, 324 students, 5 Chennai venues + Scarborough + online, per `site.ts:42-53`). Beats the Wix original and most rival sites on performance, SEO honesty, and storytelling. Not a library, not a starter, not venture-scale — never pretending to be. Don't extract a framework; finish it for the academy.

## Market Context

Competitors: Bharata Kalanjali (prestige, 1968), Kalakshetra Foundation (canonical), Kalapeetham (diaspora, 36yr), long-tail Chennai studios (Kshetra, Shivam Narthanalayam, Natya Samarpanam).
Unique Value: Mudra-choreographed scroll + hastas DefinedTermSet + derived honest structured data + enquiry route that refuses to fake success.
Recommendation: Continue — add Turnstile + durable limiter, 1 smoke test + CI build, real README, root cleanup, then ship.

## Top 3 Priorities

1. **Close the abuse gap** in `web/app/api/enquiry/route.ts:26-29` — Cloudflare Turnstile server-side verify + move `hits` map to KV/Upstash. The file tells you exactly what to do.
2. **Add one smoke test + CI**: `next build && next lint` on push + a Playwright test that homepage renders 6 acts and POST `/api/enquiry` validates/rejects bad phone. Prevents blank-homepage deploys; currently zero coverage.
3. **Write a real README** (`web/README.md:1-36` still create-next-app): setup, `npm run dev/build`, env table (`RESEND_API_KEY`, `ENQUIRY_TO/FROM`), how to edit `site.ts`/`curriculum.ts`/`classes.ts`, image pipeline for `assets/img`, deploy target. Gitignore `dev.log`/`prod.log`, add root README mapping `_research/` vs `_build/` vs `prototype/` vs `web/`, gitignore or remove 7MB `bundled_prompt.txt`.
